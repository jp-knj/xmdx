// Re-export from xmdx — canonical source moved to xmdx/utils/config
export {
  resolveExpressiveCodeConfig,
  resolveStarlightConfig,
} from 'xmdx/utils/config';
export type {
  ExpressiveCodeConfig,
  ExpressiveCodeUserConfig,
  StarlightConfig,
  StarlightUserConfig,
} from 'xmdx/utils/config';
