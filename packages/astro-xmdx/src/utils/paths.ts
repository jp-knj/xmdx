// Re-export from xmdx — canonical source moved to xmdx/utils/paths
export {
  DEFAULT_EXTENSIONS,
  stripQuery,
  normalizePath,
  deriveAstroUrl,
  deriveFileOptions,
  shouldCompile,
} from 'xmdx/utils/paths';
export type { FileOptions } from 'xmdx/utils/paths';
